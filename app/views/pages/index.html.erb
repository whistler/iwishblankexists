<div class="hero-unit">
       <h1>Iwishitexists</h1>
       <p>Helps you find what have already been done</p>
       <%= form_for(@wishname) do |f| %>
       <h3>I wish
       		<%= f.text_field :name %> 
       		exists 
       		<%= f.submit "Find out!", :class => "btn btn-large btn-primary"  %>
       </h3>
       <% end %>
     </div>
</div>