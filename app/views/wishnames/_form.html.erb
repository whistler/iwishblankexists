<%= form_for(@wishname) do |f| %>
  <% if @wishname.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wishname.errors.count, "error") %> prohibited this wishname from being saved:</h2>

      <ul>
      <% @wishname.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
